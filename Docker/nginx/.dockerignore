# 选择基础镜像
FROM node:20-alpine

# 设置工作目录为 /app
WORKDIR /app

# 将 package.json 和 pnpm-lock.yaml 复制到容器中
COPY package.json pnpm-lock.yaml ./

# 安装 pnpm
RUN npm install -g pnpm

# 安装依赖
RUN pnpm install

# 将项目的所有文件复制到容器中
COPY . .

# 构建项目
RUN pnpm run build